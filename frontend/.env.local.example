# Copy this file to frontend/.env.local
# URL the browser uses to reach the backend API directly (local dev without nginx).
# When running the full stack via docker-compose + nginx, requests go through the
# nginx proxy instead and this can be left as-is or pointed at the backend's exposed port.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
